name: campus_project
description: "CampusConnect - Smart University Management Platform"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_fonts: ^8.0.2
  dio: ^5.3.0
  shared_preferences: ^2.2.0
  stomp_dart_client: ^0.4.4
  provider: ^6.1.2
  table_calendar: ^3.1.2
  # firebase_core ve firebase_messaging google-services.json
  # kurulumu tamamlandıktan sonra aktif edilmeli:
  # firebase_core: ^3.6.0
  # firebase_messaging: ^15.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
